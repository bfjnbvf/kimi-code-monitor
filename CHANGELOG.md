# 更新说明

## v2.1.0（2026-08-03）

> 相比 v2.0.0，本版本重点调整长期统计的数据来源，简化会话数据模型，并修复宠物动画、状态显示和 popup 交互问题。

### 本地 CLI 长期统计

- 默认模式不再持久化 WebSocket 会话历史。输入、输出、缓存命中、速度、耗时和逐步曲线只维护当前页面数据；刷新或切换会话后由服务端快照与 WebSocket 重新建立
- 新增可选的“连接本地 CLI”授权，从 `~/.kimi-code/sessions/**/agents/*/wire.jsonl` 中仅提取顶层 `usage.record`，用于生成24小时、7天、30天和自定义日期范围统计
- 只保存文件偏移量、文件元数据和最近90天的按日数字汇总，不保存或上传对话原文、回答内容、工具参数和工具输出
- 首次扫描显示真实读取百分比；后续只读取新增内容，并通过单任务锁、15秒冷却和页面数据过期判断限制自动扫描频率
- 修复扫描完成通知再次触发扫描造成的循环读取、popup 状态闪烁和持续占用问题
- 未连接 CLI 时，仅长期消耗量模块和宠物“24h消耗”显示“需连接 CLI”；当前会话指标、额度和动画不受影响
- 授权入口增加 macOS、Windows、Linux 推荐路径和隐藏目录操作提示；选错目录会明确报错，授权后可重新授权或取消

### Popup 与统计口径

- popup 的长期统计可在消耗量、输入、输出、缓存命中之间切换，所选指标会保留，柱状图和日期范围同步变化
- 5h额度、本周额度及各处缓存命中率统一显示一位小数，且向下截断而非四舍五入：99.95% 显示 99.9%，真实值未达到100%不会提前显示为100.0%
- 授权状态统一为“本地记录已授权 / 额度接口已授权”，额度访问凭证自动续期，不再展示容易误解的短期到期时间
- “导出数据”改为“导出统计”，导出 CLI 按日汇总和额度快照，不包含 CLI 对话原文
- 会话折线图样本由最近40步扩至50步；每轮对话的最后一步加常驻大节点，区分轮内调用与整轮结束；修复圆点因 SVG 横向拉伸变成扁椭圆的问题，侧栏拖拽改宽后自动重绘
- 面板下边缘下移：侧栏“设置”上下间距统一为12px，面板下边缘与设置区间距对齐
- 长期统计与面板消耗量柱图按主/子代理分桶堆叠（按 CLI agents 目录名区分），popup 悬停显示“主 X · 子 Y”拆分；旧索引缓存无子代理维度，升级后自动全量重扫一次

### 宠物动画与状态

- 动作和外观彻底分离：Rive 只负责动作，蓝色、灰色和红色由当前连接/授权/限流状态独立控制，其他动作不再继承异常颜色
- 工作期间持续播放 `loading`；单次 loading 自然结束后自动续播，不再退回普通空闲动画
- 工具调用期间稳定显示“执行中”，直到全部工具结果返回；极短工具调用也至少显示1.5秒
- 工作状态细分：新增「回复中」（assistant.delta 正在输出正文）；子代理 step 与 subagent.* 生命周期事件都会正确显示「子代理工作中」，不再被 step.started 覆盖回「思考中」
- 状态体系精简为 8 个：「运行中」并入「思考中」，「执行中」更名「调用中」，「重连中」并入「未连接」（断线即提示、后台继续退避重连）；所有状态至少显示 1.5 秒，不再一闪而过
- 「子代理工作中」等超过 4 字的状态按语义切两行显示（不超 6 字），下边缘与时钟对齐
- 折线图面积填充与折线新增左右两端渐隐，边缘不再生硬截断
- 面板与 popup 消耗量柱图的主/子代理堆叠改为常态淡蓝（主）/ 淡绿（子），悬停加深，不再默认灰色
- Stars 只在同一会话真实完成一轮回答后播放，切换会话、恢复快照和页面重连不再误触发
- Mini / 完整模式切换继续随机播放轻动作，但移除会让宠物“从无到有”弹入的 `in` 动画；缩放动作池扩充至6种，直接点击动作池扩充至8种
- 空闲时低频随机眨眼或侧看，完成后回到 `paopao`；保留官网动画的自然感，但不恢复颜色与状态互相干扰的完整状态机

### 会话与可靠性修复

- 会话快照兼容新版 `{ data: ... }` 包装；全零空壳快照不再覆盖当前数据，改由 WebSocket 重建
- 切换会话时等待目标快照后再更新画面，减少指标短暂归零或显示横线
- WebSocket 临时状态帧不再参与 durable 序号去重，修复输入、输出、缓存和速度停止更新的问题
- 会话数据恢复改为本地优先：CLI 扫描顺带产出按会话汇总（`kimiCliUsageSessions`），刷新或切会话遇到空壳快照（忙碌会话 usage/last_seq 全零）时用它做底，实时事件在其上累计；每轮结束后的重扫在空闲时刷新底数，不重复计数
- 同页切换会话：面板状态（数值/折线/计时）存入内存缓存（上限 30 个会话），切回瞬时恢复、计时连续；切换时状态立即归位，订阅初期服务端补推的滞留 agent/work 状态不再把空闲会话抬成「思考中」
- 移除对 WebSocket 历史补发的依赖（实测服务端对游标落后的空闲会话会全量重放历史）：订阅应答（ack/resync_required）之前到达的事件不改状态、不播 Stars；历史重放只进折线样本（输入/输出/命中/速度/耗时曲线对历史会话照常显示），断线补发照常计数，两者都不与本地汇总双算；Stars 另保留墙钟守卫兜底
- 额度预警继续按原始百分比判断，不受显示精度影响；额度响应会等待预警状态和每日快照写入完成
- 修复内容脚本退出时新手引导监听器和 DOM 残留问题
- 工具结果返回后工作状态恢复为最新工作状态，不再被重置为“思考中”
- 速度显示改为最近 10 步的聚合速度（总输出 ÷ 总流式时长）：个别秒回 step 不再把速度顶到数百 tok/s 的虚高值
- 断开本地 CLI 授权前会先等待进行中的扫描写完，避免断开后状态被扫描结果复活；扫描失败时保留前次文件计数，不再显示归零

### 面板模块

- 新增整宽「子代理」模块（默认在隐藏区，长按进入编辑模式拖出启用）：无分隔线表格布局，输入/输出/命中三列右对齐；圆形徽标常态灰色、对应代理工作时点亮（主淡蓝、子淡绿）；徽标后直接跟模型名（深灰）；窄面板先压名称列、再由边界整体裁切。≡ 菜单可逐个开关代理的显示。模型名取自 config.update 的 modelAlias（子代理的 usage 记录只有 __secondary__ 占位符，索引升至 v4，升级后自动全量重扫一次）。不保存任何任务文本
- 新增「外部账户」模块（默认隐藏区）：DeepSeek / Kimi API 余额（赠送/充值拆分，人民币）与智谱 / MiniMax Token Plan 额度窗口（5h/1w 用量 + 重置时间）；整宽为一行一个账户的列表，半宽显示第一个选中账户的大数字（余额或用量百分比 + 次窗口角标）；≡ 菜单可逐个开关账户显示。API Key 在 popup 的「外部账户」区块添加（加号 → 选类型 → 粘贴 key，启用时按需申请域名权限），支持同一厂商多个账户，面板活跃时每分钟刷新
- 外部账户的 API Key 保密储存：AES-GCM 加密落盘，密钥以不可导出形式存于本机 IndexedDB，扩展存储中只保留密文；明文只在抓取时于内存中短暂解密使用，不上传、不随浏览器同步
- 待授权横幅与「连接本地 CLI」提醒框统一为同一格式：虚线描边 + 浅底 + 左对齐 + 副标题说明；授权框红字红框、CLI 框蓝字蓝框
- 修复浏览器重启后 popup 重复提示授权的问题：目录句柄仍在时一键恢复读取权限，不再需要重新选择目录

## v2.0.0（2026-08-02）

> 相比 GitHub 上最新版本 v1.1.1（2026-07-31）的完整更新内容。

### 面板模块化

面板由固定布局改为可配置模块。模块清单：标题行、输入、缓存命中、输出、速度、上轮耗时、5h 额度、本周额度、消耗量、宠物。

- 长按面板进入编辑模式。编辑模式下面板分为三个区域：顶部灰色区（隐藏）、中间区（仅完整模式显示）、底部区（Mini 模式保留）。模块可在区域间拖动改变归属，同区域内拖动调整顺序；拖动过程中边框颜色实时标示当前所在区域（灰/蓝/绿）
- 每个模块右上角提供 ≡ 菜单，可设置宽度（半宽/整宽）及模块专属选项
- 配置保存在 chrome.storage.local，多标签页实时同步；弹窗新增「重置布局」按钮，可将布局恢复默认（不影响历史数据）
- Mini 模式行为调整：底部区域的模块即为 Mini 显示内容，点击底部区域切换收起/展开；Mini 与完整状态边距统一为 12px
- 标题行支持配置：余额显示开关、余额点击跳转目标（充值页/控制台）

### 宠物模块

新增基于官方 Rive 动画的吉祥物模块（资产取自 kimi.com 公开 CDN，运行时为 canvas-lite 本地打包）。

- 状态动画：空闲（呼吸）、思考/执行中（loading）、限流（红脸）、子代理工作中、重连中、未连接、未授权各有对应动画；一轮回答结束时播放 stars + nostars；点击小球或迷你区播放随机一次性动作
- 时钟：空闲时显示当前时间（12 小时制 AM/PM），活跃状态显示本轮回答已用时长；思考与工具执行之间切换不打断计时
- 状态文字细化：区分「思考中」（模型生成）与「执行中」（工具调用），颜色规则：处理中蓝色呼吸、异常红色、子代理绿色、限流/重连黑色
- 右侧数据可选：24h 消耗 / 输入 / 输出 / 缓存命中 / 速度 / 余额（≡ 菜单切换）
- 点击小球可配置跳转：控制台 / 充值页 / 无跳转（默认）
- 新增状态监测：限流中（429 事件）、子代理工作中（agentId 区分）、重连中（WS 断开至重连成功或连续失败 6 次转未连接）

### 数据与统计

- 消耗量模块：按天统计 token 消耗（24h / 7d / 30d），主/子代理堆叠柱图，悬停查看单日明细
- 整宽统计模块附带会话折线图（输入/输出/缓存命中/速度/上轮耗时），悬停显示节点数值
- 5h / 本周额度条新增匀速参照竖标（按时间均匀消耗的应到位置），≡ 菜单可关闭
- 会话用量持久化：background 按会话落盘累计值、逐 step 样本与轮次耗时；索引超 6MB 按最旧会话逐出；弹窗导出 JSON 含 usageDaily、usageSessions 与额度每日快照
- 会话重建（credential 轮换 / 扩展重载）时快照缺失可回退本地记录，输入/输出不再归零
- 额度模块与余额全部隐藏时暂停每分钟额度拉取（额度预警同步停用），恢复显示即恢复

### 侧栏改造（宠物 ≡ 菜单开关，默认开启）

- 隐藏侧栏顶部品牌 logo，新建对话按钮上移并与伸缩按钮对齐
- 隐藏侧栏底部分隔线
- 宠物模块隐藏时改造自动还原

### 其他

- 点击标题强制重拉额度（绕过 background 30s 缓存）并重置面板计数
- WebSocket 新增 server_hello 看门狗（10 秒未到达主动断开重连）
- 新手引导改为分步式：气泡 + 箭头直接指向面板上的真实元素（Mini 区域、宠物、整体布局），完成最后一步自动进入编辑模式；未授权期间推迟，授权完成后补播
- 编辑模式三区域改为色块分区：灰/蓝/绿底色区块 + 色点标题（隐藏区/展开区/固定区），与引导图例用词一致
- 待授权横幅 Mini 模式下不再收起（此前折叠后无任何重新授权入口），底色改为浅红警示色
- 默认布局调整：标题行默认进入隐藏区作备份，宠物默认沉底并排在 5h/本周额度之上（已有安装的配置不受影响，弹窗「重置布局」可应用新默认）
- 状态文字呼吸动画周期放慢一倍（3.2s）
- 本月额度模块上线后下线：订阅接口仅接受 kimi.com 网页端 token（寿命约 18 分钟），中转与轮询方案体验不佳；相关代码保留备用

### 修复

- 未授权 / 掉线恢复后小球颜色停留在灰色：history_gary 动画会把球置灰且其他动画不还原颜色，恢复时改为重建 Rive 实例回到默认蓝
- 快速切换 Mini / 连续点击导致页面卡死（Rive 0 高度画布渲染、stop 事件递归）
- 拖拽排序在触摸/手势中断时监听器泄漏（补 pointercancel 处理）
- 编辑菜单宽度正反馈（越点越宽）
- 吉祥物动画位置残留、星星粒子残留、stars 衔接跳变
- 配置变更触发双重建与 Rive 双重实例化（storage 回声去重 + 实例复用）
- 会话存档剪枝与写入队列的并发竞态
- 模块高度不统一（统一为全宽 51px、半宽 37px）；窄宽度下文字降级显示替代溢出

## v1.1.1（2026-07-31）

### 修复

- 移除 `resync_required` 事件处理：该逻辑在部分会话上会导致面板输入/输出/缓存命中/速度停止更新，并可能触发重同步循环刷错；事件本身恢复为忽略（与 v1.0.x 行为一致）
- 服务器广播的供应商限流事件（429 引擎过载）不再记录为扩展错误——这是服务器端瞬时状态，会自动重试
- 其余服务器 error 事件日志改为 60 秒节流并内联 payload，避免被 Chrome 收集为扩展错误刷屏

## v1.1.0（2026-07-30）

### 新功能

- **消耗量统计**：popup 新增消耗量板块，按天累计 Kimi Code Web 会话的 token 消耗（输入/输出/缓存读取），支持任意日期范围查看（原生日期选择器），悬停柱状图可查看单日数据
- **主/子代理分维度**：柱状图按主代理（蓝）/ 子代理（绿）堆叠展示，tooltip 分别显示两者用量
- **数据导出**：footer 新增「导出数据」，可将按天累计数据下载为 JSON
- **额度预警**：5 小时 / 本周额度越过 80% 和 95% 时发送桌面通知，点击直达 Kimi 控制台（仅在页面活跃时评估，不做后台轮询）
- **快捷链接**：footer 新增「控制台」（Kimi Code Console）与「检查更新」（GitHub Releases）

### 优化

- 数值（含单位）统一使用等宽字体，与页面已加载的 JetBrains Mono 对齐
- 卡片圆角、focus ring、边框、阴影对齐 Kimi 设计规范
- 授权管理压缩为一行：状态 + 「切换 / 清除」轻操作
- 速度行的上轮耗时在窄宽度下自动隐藏「上轮」字样，分钟单位改用单字母 `m`
- 快照缺失 usage 字段时保留现有累计值，不再清零

### 说明

- 消耗量统计从本版本安装后开始累计，仅覆盖通过本扩展观察到的 Kimi Code Web 会话（不含 CLI 与其他设备）
- 首次加载时 Chrome 会提示新增「通知」权限，用于额度预警

## v1.0.3 及更早

见 Git 提交历史。
