# macOS
.DS_Store

# Node（测试与构建依赖）
node_modules/
dist/

# 旧版残留与本地预览
content 2.js
.preview.html
.preview.png
.popup-preview.png

# 打包产物
kimi-web-status-bar-v*.zip
.preview-*.html

# 打包产物（zip 归档目录 + 根目录新生成的 zip）
dist-archive/
kimi-code-monitor-*.zip
kimi-code-monitor-v*-unpacked/

# Chrome 加载解包扩展时生成
_metadata/

# docs/ 下的一次性设计稿与截图工具（仅本地保留，不进仓库）
# 只匹配 docs/ 顶层裸文件：docs/screenshots/ 里的发行截图与各 .md 照常跟踪
# （此后若要在 docs/ 顶层新增需要入库的 html/png，用 git add -f）
docs/*.html
docs/*.png

# 桌面端补丁包构建产物
kcm-desktop-patch*.zip
dist-patch/
